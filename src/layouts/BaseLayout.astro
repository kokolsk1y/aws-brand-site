---
const { title = 'AWS Products — Электрика нового стандарта', description = 'Российский бренд электрики с вниманием к деталям', ogImage = '/logo/AWS.png' } = Astro.props;
import '../styles/main.css';
import { ClientRouter } from 'astro:transitions';
---
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:type" content="website" />
    <link rel="icon" type="image/png" href="/logo/AWS.png" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
    <ClientRouter />
</head>
<body>
    <!-- SCROLL PROGRESS BAR -->
    <div class="scroll-progress" id="scrollProgress"><div class="scroll-progress__bar"></div></div>

    <!-- LOADING SPLASH — показывается только при первом заходе -->
    <div class="splash" id="splash" data-splash>
        <div class="splash__logo-wrap">
            <img src="/logo/AWS.png" alt="AWS Products" class="splash__logo" />
            <div class="splash__line"></div>
        </div>
    </div>
    <header class="header dark scrolled" id="header">
        <div class="header__inner">
            <div class="header__left">
                <button class="header__burger" id="menuToggle" aria-label="Меню">
                    <span></span><span></span><span></span>
                </button>
                <span class="header__slogan desktop-only">Качество в каждой детали</span>
            </div>
            <div class="header__center">
                <a href="/" class="header__logo">
                    <img src="/logo/AWS.png" alt="AWS Products" class="header__logo-img" />
                </a>
            </div>
            <div class="header__right">
                <a href="/#about" class="header__link desktop-only" data-astro-reload>О бренде</a>
                <a href="/#buy" class="header__link desktop-only" data-astro-reload>Где купить</a>
                <a href="/#contacts" class="header__link desktop-only" data-astro-reload>Контакты</a>
            </div>
        </div>
    </header>

    <div class="side-menu" id="sideMenu">
        <div class="side-menu__overlay" id="menuOverlay"></div>
        <div class="side-menu__panel">
            <button class="side-menu__close" id="menuClose">&times;</button>
            <nav class="side-menu__nav">
                <div class="side-menu__group">
                    <button class="side-menu__toggle">Ассортимент <span class="side-menu__arrow">&#9662;</span></button>
                    <div class="side-menu__dropdown">
                        <a href="/series/uno" class="side-menu__sub" data-preview="/img/series/uno-1kl-w.png" data-preview-title="UNO SoftTouch">Серия UNO SoftTouch</a>
                        <a href="/series/aura" class="side-menu__sub" data-preview="/img/series/aura-1kl-w.png" data-preview-title="AURA">Серия AURA</a>
                        <a href="/series/design" class="side-menu__sub" data-preview-title="DESIGN · скоро">Серия DESIGN</a>
                        <a href="/category/extensions-home" class="side-menu__sub" data-preview="/img/products/AWS-H03-1,5.webp" data-preview-title="Удлинители бытовые">Удлинители бытовые</a>
                        <a href="/category/extensions-garden" class="side-menu__sub" data-preview="/img/products/AWS-HZ2-01-15.webp" data-preview-title="Удлинители садовые">Удлинители садовые</a>
                        <a href="/category/power-connectors" class="side-menu__sub" data-preview="/img/products/SN-33.webp" data-preview-title="Силовые разъёмы">Силовые разъёмы</a>
                        <a href="/category/clamps" class="side-menu__sub" data-preview="/img/products/SN-222415.webp" data-preview-title="Клеммы">Клеммы</a>
                        <a href="/category/patch-cords" class="side-menu__sub" data-preview="/img/products/AWS-13010.webp" data-preview-title="Патч-корды">Патч-корды</a>
                        <a href="/category/cables" class="side-menu__sub" data-preview="/img/products/AWS-12982.webp" data-preview-title="Кабель">Кабель</a>
                    </div>
                </div>
                <a href="/#about" class="side-menu__item" data-astro-reload>О бренде</a>
                <a href="/#buy" class="side-menu__item" data-astro-reload>Где купить</a>
                <a href="/#contacts" class="side-menu__item" data-astro-reload>Контакты</a>
            </nav>
            <div class="side-menu__contacts">
                <a href="mailto:info@awsproducts.ru">info@awsproducts.ru</a>
            </div>
        </div>

        <!-- Preview panel (desktop only) -->
        <div class="side-menu__preview" id="sideMenuPreview" aria-hidden="true">
            <div class="side-menu__preview-img-wrap">
                <img src="" alt="" class="side-menu__preview-img" id="sideMenuPreviewImg" />
                <div class="side-menu__preview-placeholder" id="sideMenuPreviewPlaceholder">Скоро</div>
            </div>
            <div class="side-menu__preview-title" id="sideMenuPreviewTitle"></div>
        </div>
    </div>

    <main>
        <slot />
    </main>

    <footer class="footer-v2" id="contacts">
        <div class="footer-v2__inner">

            <!-- Crown: крупный слоган + email -->
            <div class="footer-v2__crown">
                <div class="footer-v2__crown-left">
                    <p class="footer-v2__kicker">Есть вопрос или запрос на партнёрство?</p>
                    <a href="mailto:info@awsproducts.ru" class="footer-v2__email">
                        info@awsproducts.ru
                        <span class="footer-v2__email-arrow">→</span>
                    </a>
                </div>
                <div class="footer-v2__newsletter">
                    <p class="footer-v2__news-label">Новости бренда</p>
                    <p class="footer-v2__news-text">Узнавайте первыми о выходе серии DESIGN и новых цветах</p>
                    <form class="footer-v2__news-form" id="newsForm" onsubmit="event.preventDefault(); this.classList.add('is-sent'); this.querySelector('.footer-v2__news-msg').textContent = 'Спасибо! Мы напишем вам.';">
                        <input type="email" placeholder="your@email.ru" required class="footer-v2__news-input" />
                        <button type="submit" class="footer-v2__news-btn">Подписаться</button>
                        <p class="footer-v2__news-msg"></p>
                    </form>
                </div>
            </div>

            <!-- Колонки -->
            <div class="footer-v2__cols">
                <div class="footer-v2__col">
                    <span class="footer-v2__col-title">Коллекции</span>
                    <a href="/series/uno">UNO SoftTouch</a>
                    <a href="/series/aura">AURA</a>
                    <a href="/series/design">DESIGN</a>
                </div>
                <div class="footer-v2__col">
                    <span class="footer-v2__col-title">Ассортимент</span>
                    <a href="/category/extensions-home">Удлинители бытовые</a>
                    <a href="/category/extensions-garden">Удлинители садовые</a>
                    <a href="/category/power-connectors">Силовые разъёмы</a>
                    <a href="/category/clamps">Клеммы</a>
                    <a href="/category/patch-cords">Патч-корды</a>
                    <a href="/category/cables">Кабель</a>
                </div>
                <div class="footer-v2__col">
                    <span class="footer-v2__col-title">Компания</span>
                    <a href="/#about">О бренде</a>
                    <a href="/#advantages">Преимущества</a>
                    <a href="/#buy">Где купить</a>
                </div>
                <div class="footer-v2__col">
                    <span class="footer-v2__col-title">Связь</span>
                    <a href="mailto:info@awsproducts.ru">info@awsproducts.ru</a>
                    <a href="https://t.me/awsproducts" target="_blank" rel="noopener">
                        <span class="footer-v2__icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21.5 4.5L2 12l7 2 2 7 3.5-5 6 4.5z"/></svg>
                        </span>
                        Telegram
                    </a>
                    <a href="https://wa.me/79000000000" target="_blank" rel="noopener">
                        <span class="footer-v2__icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M20.5 12a8.5 8.5 0 1 1-4-7.2L21 3l-1.3 5 .8 4z"/><path d="M8 10c1 2 3 4 5 5l2-2c1 0 3 1 3 1v2c0 1-1 2-2 2A10 10 0 0 1 6 8c0-1 1-2 2-2h2s1 2 1 3z"/></svg>
                        </span>
                        WhatsApp
                    </a>
                </div>
            </div>

            <!-- Gigantic word mark (SVG outline, fill-then-clip trick, без швов) -->
            <div class="footer-v2__markrow">
                <svg class="footer-v2__mark-svg" viewBox="0 0 1200 180" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
                    <text x="50%" y="140" text-anchor="middle"
                          font-family="Montserrat, sans-serif" font-weight="900" font-size="170"
                          fill="#0a0908" stroke="rgba(255,255,255,0.22)" stroke-width="2"
                          paint-order="stroke fill">aws products</text>
                </svg>
            </div>

            <!-- Bottom strip -->
            <div class="footer-v2__bottom">
                <div class="footer-v2__brand">
                    <img src="/logo/AWS.png" alt="" class="footer-v2__logo" />
                    <span>Awesome Products</span>
                </div>
                <div class="footer-v2__legal">
                    <span>&copy; 2026 AWS Products</span>
                    <span class="footer-v2__sep">·</span>
                    <a href="#">Политика конфиденциальности</a>
                    <span class="footer-v2__sep">·</span>
                    <a href="#">Оферта</a>
                </div>
                <div class="footer-v2__loc">Россия</div>
            </div>

        </div>
    </footer>

    <script is:inline>
      // Бургер-меню для подстраниц (без Lenis, без script.js главной)
      const mt = document.getElementById('menuToggle');
      const mc = document.getElementById('menuClose');
      const mo = document.getElementById('menuOverlay');
      const sm = document.getElementById('sideMenu');
      function openMenu()  { if (sm) { sm.classList.add('open'); document.body.style.overflow = 'hidden'; } }
      function closeMenu() { if (sm) { sm.classList.remove('open'); document.body.style.overflow = ''; } }
      if (mt && sm) mt.addEventListener('click', openMenu);
      if (mc && sm) mc.addEventListener('click', closeMenu);
      if (mo && sm) mo.addEventListener('click', closeMenu);
      document.querySelectorAll('.side-menu__toggle').forEach(b => {
        b.addEventListener('click', () => b.parentElement.classList.toggle('open'));
      });
      // Закрыть меню при клике на любую ссылку внутри
      document.querySelectorAll('.side-menu__item, .side-menu__sub').forEach(a => {
        a.addEventListener('click', closeMenu);
      });

      // Splash screen — ТОЛЬКО на первом "холодном" заходе. НЕ при SPA-переходах.
      (function () {
        const splash = document.getElementById('splash');
        if (!splash) return;
        const seen = sessionStorage.getItem('aws-splash-seen');
        if (seen) { splash.remove(); return; }
        sessionStorage.setItem('aws-splash-seen', '1');
        const hide = () => {
          splash.classList.add('is-hiding');
          setTimeout(() => splash.remove(), 500);
        };
        if (document.readyState === 'complete') setTimeout(hide, 900);
        else window.addEventListener('load', () => setTimeout(hide, 900));
      })();

      // Убираем splash из нового документа ДО SPA-swap — чтобы он не мигал при переходах
      document.addEventListener('astro:before-swap', e => {
        const s = e.newDocument.getElementById('splash');
        if (s) s.remove();
      });

      // Menu preview on hover (desktop)
      const previewBox = document.getElementById('sideMenuPreview');
      const previewImg = document.getElementById('sideMenuPreviewImg');
      const previewPh = document.getElementById('sideMenuPreviewPlaceholder');
      const previewTitle = document.getElementById('sideMenuPreviewTitle');
      if (previewBox) {
        document.querySelectorAll('.side-menu__sub[data-preview-title]').forEach(link => {
          link.addEventListener('mouseenter', () => {
            const img = link.dataset.preview;
            const ttl = link.dataset.previewTitle;
            if (previewTitle) previewTitle.textContent = ttl || '';
            if (img && previewImg) {
              previewImg.src = img;
              previewImg.style.display = '';
              previewPh.style.display = 'none';
            } else {
              previewImg.style.display = 'none';
              previewPh.style.display = '';
            }
            previewBox.classList.add('is-visible');
          });
        });
        const dropdown = document.querySelector('.side-menu__dropdown');
        if (dropdown) {
          dropdown.addEventListener('mouseleave', () => previewBox.classList.remove('is-visible'));
        }
      }

      // Hash scroll после View Transitions (переход /category → /#about и т.д.)
      function scrollToHash() {
        const hash = window.location.hash;
        if (!hash || hash.length < 2) return;
        const target = document.querySelector(hash);
        if (!target) return;
        const jump = () => {
          const rect = target.getBoundingClientRect();
          const y = rect.top + window.pageYOffset - 80;
          window.scrollTo({ top: y, behavior: 'smooth' });
        };
        // 3 попытки: hero большой, нужно дать время на lay-out
        jump();
        setTimeout(jump, 250);
        setTimeout(jump, 700);
      }
      // Запускаем на обычную загрузку и после каждого View Transition
      if (window.location.hash) scrollToHash();
      document.addEventListener('astro:page-load', () => {
        if (window.location.hash) scrollToHash();
      });

      // Scroll progress bar
      const progress = document.querySelector('.scroll-progress__bar');
      if (progress) {
        const updateProgress = () => {
          const h = document.documentElement.scrollHeight - window.innerHeight;
          const p = h > 0 ? (window.scrollY / h) * 100 : 0;
          progress.style.transform = `scaleX(${p / 100})`;
        };
        window.addEventListener('scroll', updateProgress, { passive: true });
        updateProgress();
      }

      // 3D tilt — для всех [data-tilt] элементов
      function initTilt() {
        document.querySelectorAll('[data-tilt]').forEach(el => {
          if (el._tiltInited) return;
          el._tiltInited = true;
          const maxRot = parseFloat(el.dataset.tiltMax) || 8;
          let rafId = null;
          el.addEventListener('mousemove', e => {
            if (rafId) return;
            rafId = requestAnimationFrame(() => {
              const rect = el.getBoundingClientRect();
              const x = (e.clientX - rect.left) / rect.width - 0.5;
              const y = (e.clientY - rect.top) / rect.height - 0.5;
              el.style.transform = `perspective(1000px) rotateX(${-y * maxRot}deg) rotateY(${x * maxRot}deg) translateY(-4px)`;
              rafId = null;
            });
          }, { passive: true });
          el.addEventListener('mouseleave', () => {
            el.style.transform = '';
          });
        });
      }
      initTilt();
      document.addEventListener('astro:page-load', initTilt);

      // Reveal on scroll — универсальный для всех элементов [data-reveal-item]
      if ('IntersectionObserver' in window) {
        const revealObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              revealObserver.unobserve(entry.target);
            }
          });
        }, { threshold: 0.15, rootMargin: '0px 0px -80px 0px' });

        document.querySelectorAll('[data-reveal-item]').forEach(el => {
          revealObserver.observe(el);
        });
      } else {
        // Fallback: если нет IO — показываем всё сразу
        document.querySelectorAll('[data-reveal-item]').forEach(el => el.classList.add('is-visible'));
      }
    </script>
</body>
</html>
